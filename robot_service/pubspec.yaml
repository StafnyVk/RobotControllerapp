name: robot_service
description: DDD package handling robot device communication over WiFi.
version: 0.0.1
publish_to: "none"

resolution: workspace

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc:
  freezed_annotation:
  json_annotation:
  get_it:
  injectable:
  http:
  web_socket_channel:
  connectivity_plus:
  shared_preferences:
  uuid:
  doso:

dev_dependencies:
  build_runner:
  freezed:
  json_serializable:
  injectable_generator:
  bloc_test:
  mocktail:
  flutter_test:
    sdk: flutter
  flutter_lints:
