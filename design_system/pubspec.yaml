name: design_system
description: Shared design tokens, theme and reusable widgets.
version: 0.0.1
publish_to: "none"

resolution: workspace

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_fonts:
  flutter_screenutil: ^5.9.3
  shared_preferences: ^2.5.5
  video_player: ^2.11.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:
