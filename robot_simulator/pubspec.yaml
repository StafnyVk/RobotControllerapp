name: robot_simulator
description: Lightweight simulated robot API (HTTP + WebSocket).
version: 0.0.1
publish_to: "none"

resolution: workspace

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  shelf:
  shelf_router:
  shelf_web_socket:
  web_socket_channel:
  args:

dev_dependencies:
  test:
  http:

executables:
  server: server
